//5
    //2. use require keyword to load the writer.js module
const writerModule = require('./writer');
    //3. invoke(call) the writeData method from the writer.js module tow rite the text to the file.
writerModule.writeData();
// Run app.js in CL